# Parameter Evidence

Track 9 adds a primary care copayment subsidy parameter.

Parameter artifact:

- `openfisca_aotearoa/parameters/entitlements/health/pae_ora/primary_care_copayment_amount.yaml`

Current modeled value:

- `2022-07-01`: `15.0`

The parameter is consumed by
`pae_ora__primary_care_copayment_subsidy`.
