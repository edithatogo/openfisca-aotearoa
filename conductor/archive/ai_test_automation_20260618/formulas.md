# Formula Evidence

Track 20 does not change legislative formulas.

The accepted fixture executes the existing `age` variable through
`BatchSimulator` and compares the deterministic OpenFisca result with the
reviewed expected output. The review gate prevents quarantined or rejected
generated scenarios from executing as accepted tests.
